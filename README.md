# Sentinel

A moderation plugin for Paper/Spigot Minecraft servers (1.21+).

## Install

Download the latest `Sentinel-x.y.z.jar` from the **[Releases](../../releases)**
page, drop it into your server's `plugins/` folder, and restart. The plugin keeps
itself up to date automatically.
